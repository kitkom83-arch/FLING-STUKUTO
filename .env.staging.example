# Placeholder template only. Do not commit real .env files or real values.
# Put real staging values in Render Environment/Secrets or another platform secret manager.
# Local ignored .env files may be used only for safe local staging/test verification.
# Rotate any value immediately if it is pasted, printed, committed, or screenshotted.

NODE_ENV="staging"
APP_ENV="staging"
PORT="4000"

# Database staging/test only. Secret manager only, never production.
DATABASE_URL="<staging-database-url>"

# Admin/session/auth. Secret manager only. Use staging-only generated values.
JWT_SECRET="<set-in-render-environment>"
LOCAL_ADMIN_PASSWORD="<set-in-render-environment>"
JWT_EXPIRES_IN="7d"

# CORS/base URL. Keep staging/test only.
CORS_ORIGIN="https://<staging-frontend-or-admin-origin>"
PUBLIC_API_BASE_URL="https://<staging-domain>"

# Provider/game mock/sandbox/disabled.
GAME_PROVIDER_MODE="mock"
GAME_PROVIDER_API_BASE_URL="<set-in-render-environment>"
GAME_PROVIDER_AGENT_CODE=""
GAME_PROVIDER_API_KEY="<set-in-render-environment>"
GAME_PROVIDER_SECRET="<set-in-render-environment>"

# Payment mock/sandbox/disabled.
PAYMENT_PROVIDER_MODE="mock"
PAYMENT_API_BASE_URL="<set-in-render-environment>"
PAYMENT_MERCHANT_ID=""
PAYMENT_API_KEY="<set-in-render-environment>"
PAYMENT_SECRET="<set-in-render-environment>"

# Bank/statement mock/sandbox/disabled.
BANK_STATEMENT_MODE="mock"
BANK_API_BASE_URL="<set-in-render-environment>"
BANK_API_KEY="<set-in-render-environment>"

# SMS mock/sandbox/disabled.
SMS_PROVIDER_MODE="mock"
SMS_API_BASE_URL="<set-in-render-environment>"
SMS_API_KEY="<set-in-render-environment>"

# Slip OCR mock/sandbox/disabled.
SLIP_OCR_MODE="mock"
SLIP_OCR_API_BASE_URL="<set-in-render-environment>"
SLIP_OCR_API_KEY="<set-in-render-environment>"

# Mock mode leaves provider credentials empty.
# Sandbox mode may use provider API URLs and credentials, but those values must be set
# in Render Environment/Secrets only and must never be committed.
# Live mode is forbidden for staging preparation.
